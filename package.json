{
  "name": "peachy-collection",
  "private": true,
  "version": "0.2.1",
  "description": "Peachy Collection is a growing family of Peachy-styled themes for editors, terminals, prompts, and the web.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "generate": "node packages/generator/build.mjs",
    "generate:check": "node packages/generator/build.mjs --check",
    "color:audit": "node scripts/color-audit.mjs",
    "validate:json": "node scripts/validate-json.mjs",
    "check": "npm run generate:check && npm run validate:json && npm run color:audit",
    "release": "node scripts/release.mjs"
  }
}
