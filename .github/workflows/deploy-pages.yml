# =============================================================
# DEPRECATED — Replaced by Cloudflare Pages (deploy-cloudflare.yml)
# =============================================================
# This workflow is disabled. The site is now deployed via
# Cloudflare Pages to peachytheme.party.
#
# Keeping this file so the git history is clean.
# To re-enable GitHub Pages, uncomment the block below and
# enable Pages in repo Settings → Pages → Source: GitHub Actions.

# name: Deploy Site (GitHub Pages)
#
# on:
#   push:
#     branches:
#       - main
#   workflow_dispatch:
#
# permissions:
#   contents: read
#   pages: write
#   id-token: write
#
# concurrency:
#   group: pages
#   cancel-in-progress: true
#
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: 20
#       - name: Generate shared assets
#         run: npm run generate
#       - uses: actions/configure-pages@v5
#       - uses: actions/upload-pages-artifact@v3
#         with:
#           path: apps
#
#   deploy:
#     needs: build
#     runs-on: ubuntu-latest
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     steps:
#       - id: deployment
#         uses: actions/deploy-pages@v4
